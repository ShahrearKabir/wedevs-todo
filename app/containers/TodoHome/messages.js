/*
 * TodoHome Messages
 *
 * This contains all the text for the TodoHome container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TodoHome';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TodoHome container!',
  },
});
